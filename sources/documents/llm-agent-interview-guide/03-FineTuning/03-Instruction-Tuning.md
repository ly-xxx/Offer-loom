# 指令微调 (Instruction Tuning)

## 1. 什么是指令微调？

### Q: SFT/指令微调的基本概念

让预训练模型学会遵循人类指令的过程。

**训练数据格式**：
```json
{
  "instruction": "请将以下英文翻译成中文",
  "input": "Hello, how are you?",
  "output": "你好，你怎么样？"
}
```

**损失函数**：只在 output 部分计算损失

$$\mathcal{L} = -\sum_{t \in \text{output}} \log P(x_t | x_{<t})$$

---

## 2. 数据质量

### Q: 指令微调数据质量 vs 数量？

**质量 > 数量**：
- LIMA 论文：仅 1000 条高质量数据就能获得好效果
- Alpaca：52K 条 GPT 生成数据
- 关键是数据的多样性和质量

### 数据来源

| 来源 | 优点 | 缺点 |
|------|------|------|
| 人工标注 | 质量最高 | 成本高 |
| GPT 蒸馏 | 量大便宜 | 法律风险 |
| 开源数据集 | 免费 | 质量参差 |
| Self-Instruct | 自动化 | 需要种子数据 |

---

## 3. 对话格式

### Q: ChatML 格式

```
<|im_start|>system
你是一个有帮助的助手。
<|im_end|>
<|im_start|>user
请介绍一下自己
<|im_end|>
<|im_start|>assistant
我是一个 AI 助手...
<|im_end|>
```

### 多轮对话训练

方式一：每轮独立计算损失
方式二：拼接多轮，只在 assistant 回复部分计算损失（推荐）

---

## 4. 微调实践

### Q: 全量微调 vs LoRA 微调选择

| 场景 | 推荐方法 |
|------|----------|
| 数据量大(>100K)，资源充足 | 全量微调 |
| 数据量中(1K-100K) | LoRA r=16-64 |
| 数据量少(<1K) | LoRA r=4-8 |
| 多任务/多租户 | LoRA（可热切换） |
| 显存受限 | QLoRA |

### Q: 微调常见问题

**灾难性遗忘**：
- 使用较小学习率（1e-5 ~ 5e-5）
- 混入少量预训练数据
- 使用 LoRA（天然缓解）

**过拟合**：
- 数据增强
- 早停 (Early Stopping)
- 增大 dropout
